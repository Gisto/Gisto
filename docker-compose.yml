version: "3.8"

services:
  web:
    build: .
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    ports:
      - "61570:61570"
    environment:
      NODE_ENV: development
      HOST: 0.0.0.0
    command: pnpm exec vite --port 61570 --host 0.0.0.0

volumes:
  node_modules:
