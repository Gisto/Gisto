pnpm lint
pnpm ts:lint
pnpm test

if [[ $(git rev-parse --abbrev-ref HEAD) != "main" && $(git rev-parse --abbrev-ref HEAD) != "release" ]]; then
  npx git-cliff@latest --output CHANGELOG.md main..HEAD && git add CHANGELOG.md
fi
