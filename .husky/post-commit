if [[ $(git rev-parse --abbrev-ref HEAD) != "main" && $(git rev-parse --abbrev-ref HEAD) != "release" ]]; then
  npx git-cliff@latest --output CHANGELOG.md && git add CHANGELOG.md && git commit --amend --no-edit
fi
